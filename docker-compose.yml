services:
  engpk:
    build: .
    container_name: engpk-courseware
    ports:
      - "3100:3000"
    env_file:
      - .env.production.local
    restart: unless-stopped
    labels:
      - "1panel.app=engpk-courseware"
      - "1panel.name=engpk-courseware"
      - "1panel.description=AI 互动课件生成引擎"
