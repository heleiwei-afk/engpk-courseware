/**
 * engpk · 共享 prompt 片段
 *
 * 7 类场景共用的"叙述量约束"等文案，集中在这里维护，避免散落。
 *
 * 决策 #3：软约束 + 监控指标。
 *   - 在 prompt 里写清楚建议字数与条数；
 *   - 不硬截断；通过 metricBus 上报实际值，由 /admin/metrics 监控。
 */

/** 引导语量约束（除图文类外都用） */
export const NARRATION_BUDGET_HINT = `【叙述量约束】
- 每条 speech 建议 ≤ 60 字；如确有必要可适当延展。
- 每个场景的 speech 总条数建议 ≤ 2；信息可通过画面/交互承载。
- 学生是这节课的主角，AI 老师只引导，不要喧宾夺主。`;

/** 图文类专用：保留较多讲解词（决策 #3 例外） */
export const NARRATION_BUDGET_ARTICLE = `【叙述量】
- 此场景为知识讲解类，可包含多条详细讲解；不必受其它场景的字数约束。`;

/** 共享 JSON 输出约束 */
export const JSON_OUTPUT_RULES = `【输出格式】
- 严格输出 JSON，不要加 \`\`\`json 代码块包裹，不要任何解释文字。
- 字符串中如出现引号，请用转义符 \\".
- 数值字段必须是数字，不能是字符串。`;

/** 学习目标抽取（讲解词内不要出现"妈妈奖励 X 元"等敏感内容） */
export const SAFETY_RULES = `【安全约束】
- 禁止生成任何违法、暴力、色情、歧视、仇恨、医疗建议等内容。
- 禁止涉及金钱奖励、家长惩罚等内容（鼓励语由系统模板池管理）。
- 全部内容请使用简体中文。`;
